<%= link_to 'Back', topic_path(@topic.id), :class => 'btn btn-success' %><br /><br />
<div class="col-sm-12 show-topic">
<h2 class="col-sm-offset-5">Update topic</h2><br />
<%= form_for @topic, url: topic_path(@topic.id),
             :html => {:id => 'edit-topic-form', :class => 'form-horizontal col-sm-6 col-sm-offset-3'} do |f| %>

    <div class="form-group">
      <%= f.label :name, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :name, class: 'form-control', placeholder: 'Name'  %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :title, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_field :title, class: 'form-control', placeholder: 'Title'  %>
      </div>
    </div>
    <div class="form-group">
      <%= f.label :content, class: 'col-sm-2 control-label' %>
      <div class="col-sm-10">
        <%= f.text_area :content, class: 'form-control', size: '24x6', placeholder: 'Write about the topics here...'  %>
      </div>
    </div>

    <div class="form-group">
      <%= submit_tag("Update", :class => 'col-sm-offset-5 btn btn-primary') %>
      <%= link_to 'Delete', delete_topic_path(@topic.id), :class => 'btn btn-danger' %>
    </div>
<% end %>
</div>